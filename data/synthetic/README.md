# Synthetic fixtures

These five hand-authored records exercise schema boundaries only. IDs and values
are artificial; no real CV, person or employer is represented. They are not a
golden set and demonstrate no ranking quality. No fixture grants an actual action.

Keep real candidate data, extracts, documents and database copies out of Git.
Add new fixtures with explicit synthetic provenance and test purpose.
