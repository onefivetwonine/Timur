# For local use run make poc-init to generate separate random DB credentials.
# This template contains no working database credential.
TIMUR_ENVIRONMENT=poc
TIMUR_DATA_MODE=synthetic
TIMUR_AI_PROVIDER=deterministic
TIMUR_EXTERNAL_AI_ENABLED=false
DATABASE_URL=
MIGRATION_DATABASE_URL=
PORT=8000
